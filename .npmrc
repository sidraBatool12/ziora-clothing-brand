; next-auth declares an optional peer on nodemailer@^7, but that line carries
; unpatched advisories. We run nodemailer@9 and never use next-auth's Email
; provider, so the peer range is intentionally ignored.
legacy-peer-deps=true
